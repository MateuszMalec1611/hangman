{
    "scripts": {
        "format": "prettier --write app",
        "start": "http-server -a localhost -p 8000 -c-1"
    },
    "dependencies": {
        "http-server": "^0.12.3"
    },
    "devDependencies": {
        "prettier": "^2.2.1"
    }
}
